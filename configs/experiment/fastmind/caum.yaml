# @package _global_
#
# CAUM on fastMIND (25% user subset of MIND-small for fast iteration)
# Usage: python src/train.py experiment=fastmind/caum framework=jax

defaults:
  - /experiment/mind/glove/caum
  - override /dataset: fastmind
  - _self_

name: fastmind_caum
