# @package _global_
#
# TCCM on fastMIND (25% user subset of MIND-small for fast iteration)
# Usage: python src/train.py experiment=fastmind/tccm framework=keras

defaults:
  - /experiment/mind/glove/tccm
  - override /dataset: fastmind
  - _self_

name: fastmind_tccm
