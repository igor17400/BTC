# @package _global_
#
# CROWN on fastMIND (25% user subset of MIND-small for fast iteration)
# Usage: python src/train.py experiment=fastmind/crown framework=keras

defaults:
  - /experiment/mind/glove/crown
  - override /dataset: fastmind
  - _self_

name: fastmind_crown
