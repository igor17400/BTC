# @package _global_
#
# LSTUR on MIND-small with frozen token-level PLM (title view only).
#
# Uses the canonical ``bert`` encoder. LSTUR has no abstract view, so
# we override ``text_field_abstract`` to skip building the unused
# abstract cache. Trainable params at train time: Conv1d(768→400) +
# AdditiveAttention over title tokens, category/subcategory
# embeddings, GRU + long-term user embedding.
#
# PyTorch only.
# Usage: python src/train.py experiment=mind/plm/lstur framework=pytorch

defaults:
  - /spec: lstur
  - /dataset: mind
  - /sampling: base
  - /encoder: bert
  - _self_

name: mind_lstur_plm
model_name: LSTUR
seed: 42

# Skip the abstract PLM cache — LSTUR uses only the title view.
encoder:
  text_field_abstract: ""

# Dataset: bridge spec inputs → dataset processing flags
dataset:
  version: small
  max_title_length: ${spec.inputs.title.max_length}
  max_history_length: ${spec.inputs.history.max_length}
  max_impressions_length: ${spec.inputs.impressions.max_length}
  embedding_size: ${spec.model.embedding.size}
  process_title: ${spec.inputs.process_title}
  process_abstract: ${spec.inputs.process_abstract}
  process_category: ${spec.inputs.process_category}
  process_subcategory: ${spec.inputs.process_subcategory}
  process_user_id: ${spec.inputs.process_user_id}

# Training: from spec
train:
  batch_size: ${spec.training.batch_size}
  num_epochs: ${spec.training.num_epochs}
  learning_rate: ${spec.training.learning_rate}
  early_stopping:
    patience: ${spec.training.early_stopping.patience}
    min_improvement: ${spec.training.early_stopping.min_improvement}

# Evaluation: from spec
eval:
  fast_evaluation: true
  batch_size: ${spec.evaluation.batch_size}

# Sampling: from spec
sampling:
  max_impressions_length: ${spec.inputs.impressions.max_length}
  strategy: ${spec.training.negative_sampling.strategy}
  random:
    replace: false

device:
  gpu_ids: [0]
  precision: float32
