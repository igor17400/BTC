# @package _global_
#
# CAUM on MIND-small with frozen token-level PLM word embeddings.
#
# Replaces the GloVe word embedding with cached BERT-token features.
# Title MHSA, entity branch, category branch, and the candidate-aware
# user model (Candi-CNN / Candi-SelfAtt / Candi-Att) all unchanged.
# Skips the abstract PLM cache (CAUM uses only the title view).
# PyTorch only.
# Usage: python src/train.py experiment=mind/plm/caum framework=pytorch

defaults:
  - /spec: caum
  - /dataset: mind
  - /sampling: base
  - /encoder: bert
  - _self_

name: mind_caum_plm
model_name: CAUM
seed: 42

encoder:
  text_field_abstract: ""

# In PLM mode the title MHSA operates on the full BERT-base hidden
# dim (768) instead of the GloVe ``embedding_size`` (300). MHSA needs
# ``embed_dim % num_heads == 0``, and CAUM's default ``num_heads=20``
# does not divide 768. Override to BERT-base's native 12 heads
# (12 × 64 = 768). Adjust to match other PLMs:
#   - BERT-base / RoBERTa / DistilBERT (768): 12  (or 8 / 16 / 24)
#   - SBERT MiniLM-L6 (384):                 12  (or 8 / 16 / 24)
#   - BERT-large (1024):                     16
spec:
  model:
    architecture:
      news_encoder:
        num_heads: 12

dataset:
  version: small
  max_title_length: ${spec.inputs.title.max_length}
  max_history_length: ${spec.inputs.history.max_length}
  max_impressions_length: ${spec.inputs.impressions.max_length}
  embedding_size: ${spec.model.embedding.size}
  process_title: ${spec.inputs.process_title}
  process_abstract: ${spec.inputs.process_abstract}
  process_category: ${spec.inputs.process_category}
  process_subcategory: ${spec.inputs.process_subcategory}
  process_user_id: ${spec.inputs.process_user_id}
  process_entities: ${spec.inputs.process_entities}
  max_entities: ${spec.inputs.max_entities}

train:
  batch_size: ${spec.training.batch_size}
  num_epochs: ${spec.training.num_epochs}
  learning_rate: ${spec.training.learning_rate}
  gradient_clip_val: ${spec.training.gradient_clip_val}
  grad_accum_steps: ${spec.training.grad_accum_steps}
  early_stopping:
    patience: ${spec.training.early_stopping.patience}
    min_improvement: ${spec.training.early_stopping.min_improvement}

eval:
  fast_evaluation: true
  batch_size: ${spec.evaluation.batch_size}

sampling:
  max_impressions_length: ${spec.inputs.impressions.max_length}
  strategy: ${spec.training.negative_sampling.strategy}
  random:
    replace: false

device:
  gpu_ids: [0]
  precision: float32
