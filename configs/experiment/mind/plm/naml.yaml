# @package _global_
#
# NAML on MIND-small with frozen token-level PLM for BOTH text views.
#
# Uses the canonical ``bert`` encoder. NAML reads two token-level
# caches (title @ max=32, abstract @ max=50) and runs its original
# Conv1d + AdditiveAttention pipeline on top — i.e. BERT tokens
# replace GloVe tokens, but NAML's per-token modelling stays intact.
# The PLM itself is frozen (encoder.trainability=frozen_cached).
#
# GPU footprint: ~6.4 GB title cache + ~10 GB abstract cache.
#
# Usage: python src/train.py experiment=mind/plm/naml framework=pytorch

defaults:
  - /spec: naml
  - /dataset: mind
  - /sampling: base
  - /encoder: bert
  - _self_

name: mind_naml_plm
model_name: NAML
seed: 42

# Dataset: bridge spec inputs → dataset processing flags
dataset:
  version: small
  max_title_length: ${spec.inputs.title.max_length}
  max_abstract_length: ${spec.inputs.abstract.max_length}
  max_history_length: ${spec.inputs.history.max_length}
  max_impressions_length: ${spec.inputs.impressions.max_length}
  embedding_size: ${spec.model.embedding.size}
  process_title: ${spec.inputs.process_title}
  process_abstract: ${spec.inputs.process_abstract}
  process_category: ${spec.inputs.process_category}
  process_subcategory: ${spec.inputs.process_subcategory}
  process_user_id: ${spec.inputs.process_user_id}

# Training: from spec, but with PLM-specific batch override.
# NAML PLM compile graph (two 768-d caches × history MHSA over (B, H) =
# (256, 50)) blows past the 40 GB GPU at the spec default bs=256 on
# JAX (XLA plans ~470 GB of intermediates, can't rematerialize below
# ~30 GB). bs=32 / eval_bs=64 fits cleanly. PyTorch fits at 256 too
# but uses the same value here for consistency.
train:
  batch_size: 32
  num_epochs: ${spec.training.num_epochs}
  learning_rate: ${spec.training.learning_rate}
  early_stopping:
    patience: ${spec.training.early_stopping.patience}
    min_improvement: ${spec.training.early_stopping.min_improvement}

# Evaluation: from spec, with PLM-specific batch override.
eval:
  fast_evaluation: true
  batch_size: 64

# Sampling: from spec
sampling:
  max_impressions_length: ${spec.inputs.impressions.max_length}
  strategy: ${spec.training.negative_sampling.strategy}
  random:
    replace: false

device:
  gpu_ids: [0]
  precision: float32
