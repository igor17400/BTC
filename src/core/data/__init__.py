"""
Core data module for NewsReX.

Subpackages:
    datasets/    — Dataset implementations (MIND, Japanese, Custom, Synthetic)
    processing/  — Data transformation pipeline (behaviors, news, vocabulary, sampling)
    encoders/    — Text-to-vector encoders (GloVe, BPEmb)
    loaders/     — Data loading, caching, and batching
    stats.py     — Dataset statistics and reporting
"""
